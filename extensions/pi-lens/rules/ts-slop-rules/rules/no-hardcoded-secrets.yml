---
id: ts-no-hardcoded-secrets
language: TypeScript
severity: warning
message: "Hardcoded secret detected — use process.env instead"
metadata:
  weight: 4
  category: security
rule:
  any:
    - pattern: "const $VAR = \"$_\""
    - pattern: "const $VAR = '$_'"
