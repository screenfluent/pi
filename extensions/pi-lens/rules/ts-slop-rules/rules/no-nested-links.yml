---
id: ts-no-nested-links
language: TSX
severity: warning
message: "Nested <a> tags are invalid HTML and cause unexpected behavior"
metadata:
  weight: 4
  category: security
rule:
  all:
    - has:
        kind: jsx_element
